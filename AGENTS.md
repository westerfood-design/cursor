# AGENTS

## Cursor Cloud specific instructions

This repository is currently an empty/blank project with only a stub `README.md`. There are no application services, dependencies, build tools, or test frameworks configured.

- **No package manager** configured (no `package.json`, `requirements.txt`, `pyproject.toml`, etc.)
- **No build system** (no `Makefile`, CI/CD config, Docker files, etc.)
- **No linting or testing** frameworks set up.

When code is added to this repository, update this section with:
1. How to install dependencies.
2. How to lint, test, build, and run the application(s).
3. Any non-obvious caveats discovered during setup.
